apiVersion: 1

datasources:
  - name: Prometheus
    # Explicit uid so provisioned dashboards can reference it deterministically
    # instead of relying on a generated one.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 2s
