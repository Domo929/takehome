[pytest]
asyncio_mode = auto
# Repo root on sys.path, so a bare `pytest` resolves llm/, mock/ and harness/ the
# same way CI and an editor's test runner do.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    live: hits a real vendor endpoint and spends money; deselected by default
